This is an HTML5 web based version of foursquare, called fortysquires.

It's open source, so you can see exactly how it works.

It's an example of:
  * HTML 5
  * Mobile features
  * Rails 3
  * Selling a web app via AppStoreHQ
